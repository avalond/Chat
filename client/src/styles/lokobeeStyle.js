/**
 * Created by kevin on 7/12/17.
 */
import React from 'react-native';
import {
  StyleSheet,
} from 'react-native';
import  Util from '../utils/util';

// splash view
export  let scrollViewStyle = StyleSheet.create({
  contentContainer: {
    width: Util.size.width,
    height: Util.size.height
  },
  backgroundImage: {
    width: Util.size.width,
    height: Util.size.height
  },
});
